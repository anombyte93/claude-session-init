name: CI

on:
  pull_request:
    branches: [main]

permissions:
  pull-requests: write

jobs:
  ci:
    uses: anombyte93/copilot/.github/workflows/reusable-ci.yml@v1
    with:
      node-version: '22'
      run-lint: false
      install-command: 'npm ci'
